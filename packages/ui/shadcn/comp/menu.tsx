import { Badge } from "../base/badge";

export function Menu() {
    return (
        <div>This is custom User Menu from the @ui/shadcn !
            Can you see it ? and does it updated realtime?
            but there is Something problem
            <Badge>hi.. </Badge>
        </div>
    )
}
